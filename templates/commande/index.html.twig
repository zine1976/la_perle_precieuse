{% extends 'base.html.twig' %}

{% block title %}commandes
{% endblock %}

{% block body %}


	<div class="example-wrapper">
<div class="ajoutprodtitre">
		<h1>Liste des commandes</h1>
		</div>
		<table class="table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Date</th>
					<th>Etat</th>
					<th>produit</th>
					<th>ML</th>

					<th>image</th>
					<th>quantité</th>
					<th>id User</th>
					<th>Nom</th>



				</tr>
			</thead>
			<tbody>
				{% for commande in commandes %}
					<tr>
						<td>{{ commande.id }}</td>
						<td>{{ commande.datecom|date('d/m/Y') }}</td>
						<td>{{ commande.etat }}</td>

{% for cp in commande.commandeProduits %}
						<td>
								{{ cp.produit.nom }}
						</td> 
						<td>{{(cp.produit.qte)}}</td>
						<td><img src="{{cp.produit.image}}" alt="" class="imgreg img-fluid thumbnail">
</td>
						<td>{{(cp.quantite)}}</td>
					 {% endfor %}

							{# {% for cu in commande.user %}

							<td>
						{{(cu.user.id)}}</td>
					<td>{{(cu.user.id)}}</td>
					 {% endfor %} #}
							
</tr>
                  
				
			{% endfor %}
		</tbody>
	</table>
</div>{% endblock %}