{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <h1>Mon compte</h1>
<div class="compteperso">
		<div class="col-4">


			<img src="{{asset("gris.jpg")}}" alt="" class="img-fluid thumbnail">
		</div>
    <table class="table">
        <tbody>
				{% if is_granted('ROLE_ADMIN') %}

            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
				{% endif %}
           
            
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.Prenom }}</td>
            </tr>
             <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            
            
        </tbody>
    </table>
</div>
    <a href="{{ path('app_user_edit', {'id': user.id}) }}">modifier mes données</a>
				{% if is_granted('ROLE_ADMIN') %}


    <a href="{{ path('app_user_index') }}">liste</a>


    <a class="btn btn-danger">{{ include('user/_delete_form.html.twig') }}</a>
				{% endif %}


{% endblock %}
